# ttc2ttf

Simple Python script to change `ttc` files to `ttf`.

## Usage

1. Put your `ttc` files inside `./ttc`.
2. Run `uv run main.py`.
3. Created `ttf` files are saved under `./ttf`.
4. Run `./ttc/to_woff.sh` to change created `ttf` files to `woff2`.
